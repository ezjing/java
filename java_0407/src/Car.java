public class Car {
    // Car 직접만든 클래스, 다른클래스에서 이용할 목적으로 만든것, main() 메소드를 가지고 있는 클래스에서 실행한다
    // 필드 : 객체의 데이터가 저장되는 곳(고유데이터, 현재데이터, 부품데이터 有)(변수와 동일, 소속이 있는 변수)
    String company = "기아자동차"; // 필드 선언과 동시에 초기값 설정
    String model = "포르테";
    String color = "회색";
    int maxSpeed = 250;
    int speed; // 필드를 선언만 함
}
